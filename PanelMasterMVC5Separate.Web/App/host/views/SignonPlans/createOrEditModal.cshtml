<div>
    <form name="userCreateOrEditForm" role="form" novalidate class="form-validation">
        <div class="modal-header">
            <h4 class="modal-title">
                <span ng-if="vm.job.id">@L("EditRepairType"): {{vm.job.description}}</span>
                <span ng-if="!vm.job.id">@L("ActivateRepairType") : {{vm.job.description}}</span>
            </h4>
        </div>
        <div class="modal-body">
            <uib-tabset class="tab-container tabbable-line">
                <uib-tab heading="@L("RepairTypeProperties")">
                      
                    <div class="form-group form-md-line-input form-md-floating-label no-hint">
                        <label>@L("Planname")</label>
                        <input ng-model="vm.job.planName" required name="planName" class="form-control" type="text" />
                    </div>
                    <div class="form-group form-md-line-input form-md-floating-label no-hint">
                        <label>@L("Price")</label>
                        <input ng-model="vm.job.price" required name="price" class="form-control" type="text" />
                    </div>
                    <div class="form-group form-md-line-input form-md-floating-label no-hint">
                        <label>@L("HeaderColor")</label>
                        <input ng-model="vm.job.headerColor" required name="headerColor" class="form-control" type="text" />
                    </div>
                    <div class="form-group form-md-line-input form-md-floating-label no-hint">
                        <label>@L("Members")</label>
                        <input ng-model="vm.job.members" required name="members" class="form-control" type="number" />
                    </div>
                </uib-tab>
                 
            </uib-tabset>
        </div>
        <div class="modal-footer">
            <button ng-disabled="vm.saving" type="button" class="btn btn-default" ng-click="vm.cancel()">@L("Cancel")</button>
            <button type="submit" button-busy="vm.saving" busy-text="@L("SavingWithThreeDot")" class="btn btn-primary blue" ng-click="vm.save()" ng-disabled="userCreateOrEditForm.$invalid"><i class="fa fa-save"></i> <span>@L("Save")</span></button>
        </div>
    </form>
</div>