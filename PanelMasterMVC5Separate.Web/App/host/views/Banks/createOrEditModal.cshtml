@using PanelMasterMVC5Separate.AdminFunctions
<div>
    <form name="userCreateOrEditForm" role="form" novalidate class="form-validation">
        <div class="modal-header">
            <h4 class="modal-title">
                <span ng-if="vm.bank.id">@L("EditBank"): {{vm.bank.bankName}}</span>
                <span ng-if="!vm.bank.id">@L("AddBank")</span>
            </h4>
        </div>
        <div class="modal-body">
            <uib-tabset class="tab-container tabbable-line">
                <uib-tab heading="@L("BankInformation")">
                    <div class="form-group form-md-line-input form-md-floating-label no-hint">
                        <label>@L("Country")</label>
                        <select ng-model="vm.bank.countryId" class="form-control" ng-options="opt.id as opt.name for opt in countryList" required>
                            <option value="">@L("SelectOne")</option>
                        </select>

                    </div>

                    <div class="form-group form-md-line-input form-md-floating-label no-hint">
                        <label>@L("BankName")</label>
                        <input class="form-control" type="text" ng-class="{'edited':vm.bank.bankName}" ng-model="vm.bank.bankName" maxlength="128" required>

                    </div>
                </uib-tab>

            </uib-tabset>
        </div>
        <div class="modal-footer">
            <button ng-disabled="vm.saving" type="button" class="btn btn-default" ng-click="vm.cancel()">@L("Cancel")</button>
            <button type="submit" button-busy="vm.saving" busy-text="@L("SavingWithThreeDot")" class="btn btn-primary blue" ng-click="vm.save()" ng-disabled="userCreateOrEditForm.$invalid"><i class="fa fa-save"></i> <span>@L("Save")</span></button>
        </div>
    </form>
</div>